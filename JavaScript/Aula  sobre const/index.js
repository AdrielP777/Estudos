// + - * /

const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado + 50
let resultadoTriplicado = resultadoDuplicado + 200; 

console.log(resultadoTriplicado);
console.log(typeof primeiroNumero); // tag para saber que e uma string ou number, user a tag typeof
